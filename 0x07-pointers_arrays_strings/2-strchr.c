#include <string.h>
/**
 * _strchr -fxn
 * @s:string to be searched
 * @c:charcter to be searched for in string
 * Return:char if found otherwise null
 */
char *_strchr(char *s, char c)
{
	if (char strchr(char *s, char c))
		return (s);
	else
		return (NULL);
}
